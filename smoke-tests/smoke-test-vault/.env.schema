# @plugin(./plugins/vault-plugin/)
# @initVault(addr=https://mock-vault:8200, token=mock-token)
# @defaultSensitive=false
# ---
DB_ALL=vault("myapp/database")
DB_PASS=vault("myapp/database", "password")
